{% extends 'base.html.twig' %}

{% block title %}{{parent()}} | Add Book{% endblock %}

{% block body %}
    <h1> Add Book </h1>
    <div id="formContainer">
        <div class="form-group col-md-4 center" id="formCard">
            {{ form_start(add_book_form)}}
            {{ form_widget(add_book_form)}}
            <input type="submit" value="ADD Book" class="btn btn-dark" />
            {{ form_end(add_book_form)}}
        </div>
    </div>
    

{% endblock %}
